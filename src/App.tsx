import React from 'react';
import {Hyperspace } from './Hyperspace'

interface AppProps {}

export const App = ({}: AppProps) => {
  
  return (
    <div className="App">
      <Hyperspace />
    </div>
  );
}

export default App;
